Your goal is to commit all the changes until tha last commit.

Do the following:
1. Run through all the modified files and elaborate a text explaining all the changes made so far;
2. With this text, check the documentation on docs folder if it needs an update. If it does, then update.
3. Check the roadmap if any of the criteria has been archived, if it has then update the roadmap docs as well
3. Add the files with git command and commit changes.
4. Print to the user every step been made.
 
